<div class="journalist">
  <%= link_to '← back to the list', journalists_path %>
  <h1><%= @journalist.full_name %></h1>
  <div class="photo"><%= image_tag @journalist.photo_url %></div>
  <div class="widget">
    <script src="http://<%= Rails.env.production? ? 'www.uncoverage.com' : 'localhost:3000' %>/assets/widget/widget_code.js"></script>
    <iframe class="uncoverage_widget" id="<%= Rails.env.production? ? @journalist.uncoverage_id : '538232c25679610408210000' %>" custom_widget_css="http://<%= Rails.env.production? ? 'widgetexample.herokuapp.com' : 'localhost:3001' %>/custom_uncoverage_widget_black.css" src="http://<%= Rails.env.production? ? 'www.uncoverage.com' : 'localhost:3000' %>/widget?journalist_id=<%= Rails.env.production? ? @journalist.uncoverage_id : '538232c25679610408210000' %>&amount=100"/></iframe>
  </div>
  <h2><%= @journalist.first_name %>'s biography</h2>
  <% 10.times do %>
    <div class="bio"><%= @journalist.biography %></div>
  <% end %>
</div>
